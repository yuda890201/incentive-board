rules_version = '2';

// Firebase コンソール > Storage > ルール にこの内容を貼り付けて公開してください。
// 匿名認証済みユーザーのみレシート画像のアップロード・閲覧を許可します。
service firebase.storage {
  match /b/{bucket}/o {
    match /receipts/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
